# Normalize line endings. Keep LF for everything so CI shell scripts and
# config files behave the same on Windows, Linux, and inside containers.
* text=auto eol=lf

*.py    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.sh    text eol=lf
*.md    text eol=lf
Dockerfile text eol=lf
